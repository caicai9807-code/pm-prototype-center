# 2026-07-23 工作日志

## 整改任务列表 - 状态决定操作列

`prototypes/rectification/rectification-task.html` 的 `renderAction(row)` 按状态分支渲染操作列：
- 待整改 → 详情 ｜ 接收
- 整改中 / 已超期 → 详情 ｜ 整改
- 待专家审核 → 详情 ｜ 审核（`<a href="./rectification-detail.html?...">` 直接跳转）
- 已完成 → 详情

按钮实现方式：
- 跳转类（详情、审核）：用 `<a href>` 锚点，浏览器自然跳转，不绑 JS
- 弹窗类（接收、整改）：用 `<span>` + data-* 属性，在 `bindActionLinks` 里 addEventListener

## 审核指派弹窗 - App→PC 修复

`prototypes/rectification/pages/my-task-list.js` 的 `renderAuditModal` 按 App 端设计重写：
- 修复字段名 bug：`ti.reason`/`ti.time` → `ti.rejectReason`/`ti.rejectTime`（显示 undefined 的根因）
- 补"原专家 + 所属部门"行（原专家用 `ti.originalExpert.name` + `.type` + `.dept`）
- 新增"任务调整日志"区块（蓝色时间圆点 timeline）
- 新增"选择新专家"列表（卡片 + 首字头像 + radio）
- 按钮文案"确认指派" → "确认调整"

CSS 类已就绪（`pages/my-task-list.css`），无需新增。

数据来源：`task.transferInfo`（含 originalExpert / rejectReason / rejectTime / logs）和 `task.availableExperts`（含 name / type / dept / recentTasks）。