# 2026-07-06 工作日志

## 根据截图调整专家App整改任务页面

### 修改内容

1. **expert-app-task-list.html（整改任务列表）**
   - 整改任务Tab从"待复查/已归档"改为"待整改/已归档"
   - rectificationTasks数据增加字段：dept（所属管理部）、address（用热地址）
   - rt-card卡片增加：所属管理部、用热地址字段
   - 按钮从"复查审核/查看详情"统一改为"查看整改"
   - 搜索placeholder按类型切换：面积检查→"搜索任务名称"；整改任务→"搜索站点名称"

2. **expert-app-site-detail.html（站点详情）**
   - 新增60218B004（裕华区万达站）Mock数据，含检查记录单不合格结论
   - 顶部状态卡增加：不合格标签（红色）、检查完成时间、确认进度、站点状态
   - 对于检查不合格且待整改的站点，显示"审核"按钮
   - Tab切换样式改为Pill风格（蓝底白字选中态，浅灰底黑字非选中态）
   - 审核弹窗从居中Modal改为底部Sheet弹窗：
     - 标题"复查结果"
     - 单选：通过/驳回
     - 驳回原因textarea（0/300字数计数）
     - 取消/提交按钮
     - 点击背景关闭

3. **expert-app.html（工作台首页）**
   - 整改任务统计标签从4个改为2个：待整改(5)、已归档(1)

4. **expert-app-standalone.html（独立版App）**
   - APP_RECTIFY_TASKS数据增加dept和address字段
   - renderRectifyList：Tab改为"待整改/已归档"，卡片增加字段，按钮改为"查看整改"
   - renderSiteDetail：增加顶部状态卡信息和审核按钮
   - 添加底部Sheet审核弹窗HTML和JS函数

### 文档更新

- **页面清单.md**：更新专家App·任务列表和站点详情的描述
- **字段字典.md**：更新整改任务Mock数据字段和移动端状态枚举
- **业务流程.md**：增加专家App整改任务列表流程和底部Sheet审核弹窗流程，新增3条待确认项
- **交互规则.md**：更新搜索栏、segmentTab、审核按钮、Sheet弹窗的交互规则，新增3条待确认项

### 修复Tab切换（平铺→切换）

5. **expert-app-site-detail.html（Tab 平铺修复）**
   - 问题：点击 tab 无切换效果，所有面板平铺展示
   - 根因：`.tab-panel` 缺少 `display: none` 样式，虽有 JS 切换 `.active` 类，但无 CSS 控制显隐
   - 修复：添加 `.tab-panel { display: none; }` + `.tab-panel.active { display: block; }`
   - 优化：增加 `tabFadeIn` 淡入动画（opacity + translateY），切换更平滑

### 整改页面改为只读模式

文件：`prototypes/rectification/expert-app-site-detail.html`

1. **状态卡按钮**：`checked` / `rectified` 状态的"查看检查记录单"改为"审核"按钮，调用审核弹窗
2. **建筑物卡片**：
   - 移除"实测复核"按钮 + 实测复核弹窗（HTML/CSS/JS 全部清除）
   - 改为静态"已复核"徽标（`bld-review-badge`），反显建筑面积
3. **平面图/门头图 Tab**：
   - 移除"上传现场检查照片"按钮及相关样式
   - 改为已上传照片列表展示（文件名 + 时间地点水印）
4. **整改通知单 Tab**：移除内部的"审核"按钮

### 同步更新4份文档

基于原型变更做增量更新，并新增待确认项：

1. **页面清单.md**：更新 expert-app-site-detail.html 页面描述（只读模式/已复核徽标/已上传照片列表/审核按钮）；更新审核弹窗清单条目（触发时机改为顶部状态卡）；新增2条待确认项
2. **字段字典.md**：更新 13.6 站点详情 Tab 描述（建筑物/平面图/门头图/整改通知单）；移除实测复核弹窗字段；新增已上传照片展示字段；更新待确认项
3. **业务流程.md**：更新专家App流程步骤6-10（只读模式/静态徽标/照片列表/审核按钮迁移）；更新分支流程（新增整改页面只读模式分支）；更新待确认项
4. **交互规则.md**：更新 10.8 站点详情交互规则（Tab切换动画/建筑物已复核徽标/平面图门头图上移按钮/审核按钮迁移）；更新专家App待确认项

### 页面跳转检查

所有页面跳转均为相对路径，验证通过：
- expert-app.html → expert-app-task-list.html?type=rectification ✓
- expert-app-task-list.html → expert-app-site-detail.html?code=xxx ✓
- expert-app-site-detail.html → history.back() ✓

### 创建部门领导App框架

新增2个文件：

1. **dept-leader-app-standalone.html**（独立App单文件）
   - 手机壳框架（刘海+状态栏+Home条）
   - 5个页面：工作台 / 转派审核 / 审核指派 / 整改任务 / 站点详情
   - Mock数据：3条转派任务（含原专家/拒绝原因/时间）、4条整改任务、3个管理部可用专家池
   - 核心功能：转派列表搜索 + 审核指派（查看原专家信息+选择新专家+确认转派）+ 整改列表segment切换
   - CSS类名 .dl- 前缀隔离、rt-card/app-entry-card 复用专家App样式

2. **dept-leader-app-preview.html**（PC端预览框架）
   - 左侧iPhone模型机内嵌 iframe
   - 右侧说明面板（角色定位/页面导航/页面清单/待补充）
   - 顶部操作栏（独立打开/刷新）

3. **页面清单.md** 已更新：新增2个页面条目 + 跳转关系图

### 部门领导App关联到整体框架

1. **config.js**：菜单配置更新，"部门领导App" href 从 `./index.html` 改为 `./dept-leader-app-preview.html`，并添加 key + pageMap 条目
2. **app.js**：注册 `dept-leader-app-preview` 页面模块
3. **dept-leader-app-preview.html**：重写为框架化版本（加载 config.js + app.js + layout.js + router.js + pages/*.js），iframe 内嵌 dept-leader-app-standalone.html
4. **pages/dept-leader-app-preview.js**：新创建页面模块（渲染左侧手机模型 + 右侧文档面板）
5. **dept-leader-app-standalone.html**：独立App保持不变

### 替换转派审核页为面积检查任务列表

- 转派审核页 → **面积检查任务**列表（部门领导视角）
- 新增 Segment：**全部/待处理/已处理** 三档筛选
- 新增6条 Mock 数据，含 period（任务周期）、normal/pending/done 三种状态
- 卡片展示：站点名称 + 状态Tag + 任务名称 + 任务周期 + 所属管理部 + 原专家 + 拒绝原因（待处理）/+ 已转派给+处理时间（已转派）
- 正常任务（无转派）显示"正常"蓝色Tag，无操作按钮
- 首页统计改为：全部/待处理/已处理
- 预览页文档面板同步更新

### 简化部门领导App standalone页面

- 移除手机壳框架（app-phone-shell/notch/statusbar），避免与preview页面iframe双重包裹
- 移除所有"一类专家"、"二类专家"相关字眼（6处数据 + 2处渲染模板 + 6处可用专家池）

### 修复部门领导App HTML结构

- standalone页面底部出现JS代码被渲染的问题
- 根因：删除右侧说明区时，残留了`app-phone-screen`/`app-phone-shell`的`</div>`闭合标签和损坏的`<div class="app </div>`标签
- 修复：清除第148-153行的多余/损坏标签，使页面结构正确闭合

### 同步更新4份文档（部门领导App）

基于部门领导App变更做增量更新：

---

### 输出功能清单（上午）
- 读取工作区文件输出系统完整功能清单
- 按 PC 端和 App 端分类，一级功能→二级功能→功能描述三层结构
- 用户要求增加页面描述和业务流程后补充详细版

### 输出业务流程图（下午）
- 使用 Visualizer（SVG）输出4张业务流程图
- 图1：**业务主流程全景图**——四阶段端到端（任务创建→检查执行→整改审核→整改追踪），标注合格/不合格分支、驳回回退
- 图2：**面积检查任务管理流程**——PC端管理员纵向流程（新建→基础信息+站点→抽选专家→提交），右侧编辑范围控制面板
- 图3：**专家检查执行流程**——六阶段（任务确认→开始检查→检查结论→整改通知单→双专家确认→实测复核），含确认/拒绝、合格/不合格分支
- 图4：**整改流程闭环**——左侧整改通知书审核（含≤30天自动通过规则），右侧整改任务四状态流转+操作说明+审核弹窗规则

1. **页面清单.md**：更新部门领导App页面描述（转派审核→面积检查任务、去手机壳、去一类二类专家）；重写跳转关系图（全部/待处理/已处理segment+正常/待处理/已转派三态）；新增部门领导App待确认项（5条）
2. **字段字典.md**：新增"十四、部门领导App"章节（14.1 面积检查任务列表字段 + 14.2 审核指派页字段 + 14.3 转派任务Mock数据结构）；新增待确认项（3条）
3. **业务流程.md**：新增"九、部门领导App流程"章节（主流程5步+分支流程3条+待确认项4条）
4. **交互规则.md**：新增"部门领导App待确认项"（4条+1条已确认）

### 去掉首页，进入系统直接跳转任务管理页面

1. **index.html**：替换为 `window.location.replace('./task-list.html')` 重定向页，不再显示模块概览和导航卡片
2. **pages/index.js**：清空渲染逻辑，`render()` 返回空字符串

### 创建统一数据层 PrototypeStore 实现数据闭环

**新增文件：`store/PrototypeStore.js`**
- 统一管理6类数据：tasks/taskSites/expertAssignments/checkRecords/rectificationNotices/rectificationTasks
- 状态机校验：任务草稿→待开始→进行中→已结束；站点待检查→检查中→已检查/不合格→待整改→已归档；通知书待审核→审核通过/驳回；整改任务待整改→待复查→已归档
- localStorage 持久化 = 刷新不丢
- 种子数据从 MockData 导入（首次运行）
- 自动生成 nextId

**修改页面模块（数据源从独立Mock改为PrototypeStore）：**
1. **task-list.js**：删除操作调用 store.deleteTask()，不再只是 ?delete=id；展示从 store.getTasks() 读
2. **task-create.js**：暂存调用 store.createTask()/updateTask()；下一步保存基础信息+站点到store
3. **task-expert.js**：提交调用 store.setExpertAssignments()+store.transitionTask(…,'待开始')，真正推进任务状态
4. **my-task-list.js**：数据从 store.getMyTasks() 读取（动态计算专家视角状态）；确认/拒绝调用 store.confirmExpert()/rejectExpert()
5. **my-task-check.html**：检查记录提交调用 store.saveCheckRecord()，自动推进站点状态→不合格→生成整改通知单→生成整改任务
6. **rectification-task.html**：数据从 store.getRectificationTasks() 读取；详情链接增加 taskId 参数；页面支持 ?taskId=X&siteCode=XX 筛选
7. **review-notice.html**：数据从 store.getRectificationNotices() 读取；审核调用 store.reviewNotice() 持久化结果
8. **task-detail.js**："查看整改任务"跳转带 taskId 参数

**所有框架页面 + 自包含页面均添加了 store 脚本引用**（13个HTML文件）

### 闭环断点修复（3个闭环）& 站点详情Tab对齐

1. **闭环断点修复**：通过 localStorage/sessionStorage 模拟端到端数据同步
   - 转派闭环：dept-leader-app-standalone.html 确认转派写入 `transferDone_{taskId}` → PC端 my-task-list.js 清除 hasTransfer
   - 确认闭环：expert-app-task-confirm.html 确认/拒绝写入 `taskConfirm_{taskId}` → PC端同步任务状态
   - 检查整改联动：expert-app-check-record.html 不合格结论写入 `needRectify=true` → 站点详情页显示整改通知单Tab
2. **站点详情Tab内容对齐**：dept-leader-app-standalone.html 新增完整7Tab（基础信息/面积汇总/建筑物/平面图/门头图/检查记录单/条件整改通知单）+ Mock站点数据 + Tab切换函数
3. **审核指派向PC端对齐**：任务摘要字段改为6字段（任务名称/所属年度/任务周期/所属管理部/待处理站点数），新增转派日志时间线（6条任务统一添加 year/pendingCount/logs 字段）

### 整改任务流程改造 + 整改通知书审核「详情」跳转 + 4份文档增量同步

**原型代码变更（本轮）：**
1. **rectification-task.html**：整改流程改为 待整改 →（接收确认，开始计时）→ 整改中 →（超期派生"已超期"）→（保存整改信息）→ 待专家审核 →（双专家会签）→ 已完成；新增「接收」按钮（待整改行）打开接收确认弹窗；「整改」按钮改为仅整改中/已超期行；状态Tag 5色（待整改橙/整改中蓝/待专家审核紫/已完成灰/已超期红）。
2. **rectification-detail.html**：顶部按钮仅「待专家审核」时显示「专家审核」（原为「审核」）；审核弹窗改为双专家会签（专家A 张明远 + 专家B 李建华，均同意→已完成，任一不同意→退回整改中）。
3. **review-notice.html**：操作列新增「详情」链接（任意行，跳转 `site-detail.html?code={换热站编码}`，新标签页），保留「审核」按钮（仅待审核行，弹窗不变）。曾因多行JS字符串模板漏 `\` 续行符导致页面空白，已修复并用 `node --check` 验证。

**4份文档增量同步（仅改变更点，标待确认项）：**
- **页面清单.md**：review-notice 下游改为 site-detail.html?code、操作列 详情(任意行)+审核(仅待审核)；rectification-task 新增接收确认弹窗、状态5色、操作 详情/接收/整改；rectification-detail 审核→专家审核(双专家会签)；跳转图 + 弹窗清单同步。
- **字段字典.md**：8.2 操作列、9.1/9.2 状态枚举、新增 §9.4 接收确认弹窗字段、整改任务状态枚举表（待整改/整改中/待专家审核/已完成/已超期）、10.3 双专家会签字段（expertAResult/expertAOpinion/expertBResult/expertBOpinion/countersignResult）。
- **业务流程.md**：§四 2c 详情改为查看站点详情（跳转 site-detail.html?code，新标签页）；新增待确认项（详情新标签页 vs 当前页跳转）。
- **交互规则.md**：§七 7.2 详情按钮改为跳转（替代原只读弹窗）+ 待确认（新标签页 vs 当前页）；§八 8.2 新增接收按钮、整改按钮触发条件调整（保存后→待专家审核）；§八 8.4 操作按钮显隐表替换为5状态 + 新增"接收"列；§九 9.1 审核弹窗联动→双专家会签联动（含退回整改中分支）；重点关注区补齐整改流程说明；待确认项新增3条（详情跳转方式/接收计时规则/会签不一致分支）。

**注意**：当前日期为 2026-07-06。PrototypeStore 状态机文档曾记"整改任务待整改→待复查→已归档"，与本轮原型"待整改→整改中→待专家审核→已完成"不一致，本轮文档已按新流程更新，但 `store/PrototypeStore.js` 状态机可能仍需对齐（待确认）。
