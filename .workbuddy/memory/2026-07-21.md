# 2026-07-21

## 创建项目样式规则输出

- 创建 `docs/style-guide.md`（522行）：中后台原型样式规则参考文档，包含设计Token、页面骨架、13种组件速查、常用布局组合、开发清单
- 创建 `pc-template-base.css`（1063行）：无前缀命名方案的通用CSS模板，可直接复制到新项目使用
- 采用"无前缀"命名方案（`.card`/`.btn`/`.table` 等），统一两套旧方案为一份标准模板
