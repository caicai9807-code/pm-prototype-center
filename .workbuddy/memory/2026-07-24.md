# 2026-07-24 工作日志

## 图片水印功能实现

### 需求
在原型中涉及上传图片的功能增加水印，水印内容：`时间 地点 站点+站点编码-楼栋名称`，使用占位示例数据。

### 修改的文件（共 4 个）

1. **prototypes/rectification/my-task-check.html**（非标注版）
   - 新增 `applyWatermark()` 水印工具函数（Canvas 实现，右下角半透明黑色圆角条 + 白色文字）
   - 在实测复核照片上传的文件 input 增加了 `data-site-code` 和 `data-building-name` 属性
   - 修改 file change 事件：选择照片后自动叠加水印并显示缩略图预览
   - 新增 `.mc-ver-preview` 预览容器

2. **prototypes/rectification-annotated/my-task-check.html**（标注版）
   - 同上

3. **prototypes/rectification/expert-app-site-detail.html**（非标注版）
   - 新增 `applyWatermark()` 水印工具函数
   - 修改 `handleReviewPhotoSelect()`：选择照片后自动叠加水印再存入 `reviewPhotos`
   - 静态"已上传照片"区域：`.uploaded-photo-thumb` 加 `position: relative; overflow: hidden;` + `.wm-mock` 水印示意标签，显示"裕华中心站"

4. **prototypes/rectification-annotated/expert-app-site-detail.html**（标注版）
   - 同上

### 未修改的页面
- `expert-app-building-detail.html` — 只有 `.photo-upload` CSS 定义，无实际上传功能
- `rectification-detail.html` — 只有附件文件名文字展示，无图片上传

### 水印效果
- 上传照片后，Canvas 将图片缩放到最大 1600×1200
- 在右下角叠加半透明黑色圆角矩形背景 + 白色粗体文字
- 样例内容：`2026-07-24 09:40 裕华区中心站+60218B001-1号楼(住宅)`
