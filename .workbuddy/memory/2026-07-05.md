# 2026-07-05

- 根据截图调整了 `prototypes/rectification/my-task-check.html` 的任务基本信息卡片样式：
  - 卡片头部改为任务名称 + 状态标签，右侧放置「返回我的任务」按钮；
  - 信息网格改为 3 列 × 2 行；
  - 字段改为：所属专家、所属年度、任务状态、任务周期、下发时间、当前进度；
  - 同步更新了 mock 数据，与截图示例保持一致（任务名「2026 第27周 裕华片区常规面积核查任务」、专家「赵立成」、周期 2026-07-01 至 2026-07-07 等）。
- 移除了已失效的 `.mc-page-header` 相关样式。
- 根据 UI 规范文字说明修复了 my-task-check.html 的完整业务结构：
  - 站点状态体系从 3 种扩展为 5 种（已检查/检查中/待整改/已发送待整改/待检查），各状态配置独立 Tag 颜色；
  - 站点 Tab 右侧新增统计信息栏（当前进度、已检查、检查中、待整改、待检查计数）；
  - mock 站点数据从 4 个扩展为 8 个，覆盖全部 5 种状态；
  - 为新增站点补充了面积对比数据和普查明细数据；
  - 站点 Tab 区域独立为单独的 `.mc-station-card` 卡片，与内容 Tab 卡片解耦；
  - 进度更新为 "2/8 站点已完成"。
- 后续进一步精简：
  - 站点数量从 8 个缩减为 3 个；
  - 进度更新为 "1/3 站点已完成"；
  - 清理了已移除站点对应的面积对比和普查明细 mock 数据。
- 按截图要求调整站点卡片：
  - 卡片标题「检查站点」与统计信息（当前进度、已检查、检查中、待整改、待检查）置于同一行；
  - 站点 Tab 下方新增「站点信息」面板，展示站点名称、用户编码、简称、用热地址、所属管理部、所属片区所、行政区、办事处、联系人、联系方式、普查人员、普查日期、未入网建筑物数量；
  - 切换站点时同步更新站点信息面板内容；
  - 为 3 个站点补充了站点详情 mock 数据。
- 参考 site-detail.js 向 my-task-check.html 注入普查信息模块缺失组件：
  - 普查信息 Tab 新增子Tab系统：汇总表（面积对比+普查明细）/ 平面图（占位卡片）/ 门头图片（占位卡片）；
  - 普查明细表新增第 17 列「操作」列（详情链接）；
  - 新增普查详情弹窗（14字段 Grid + 变更详情 6列表格）+ 绑定事件；
  - 站点切换时同步更新普查信息子Tab面板并重新绑定事件。
- 已同步更新 docs/ 下 4 个文档做增量修改：
  - 页面清单.md：更新 my-task-check.html 行描述 + 弹窗清单新增普查详情弹窗；
  - 字段字典.md：更新 6.2/6.3/6.5/6.7 节 + 新增 6.9 站点信息面板字段 + 6.10 普查详情弹窗字段 + 更新 7.3 状态枚举；
  - 业务流程.md：更新三、专家执行检查流程（10 阶段）+ 待确认项补充；
  - 交互规则.md：更新 6.1/6.2/6.6 节 + 新增 6.7 子Tab切换 + 6.8 详情弹窗规则。
- 本地预览地址：http://localhost:8765/prototypes/rectification/my-task-check.html
- 重构检查记录单为弹窗模式：
  - 检查记录单 Tab 改为「填写检查记录单」按钮，点击弹出大弹窗（820px）；
  - 弹窗包含7项检查项，每项支持：是/否（部分含"不适用"）radio → 选"否"展开子项checkbox多选 + 其他问题文本输入；
  - 检查项1：3个子项；检查项2：6个子项；检查项3：11个子项；检查项4：无子项（是/否）；检查项5：6个子项；检查项6：3个子项+不适用；检查项7：3个子项+不适用；
  - 弹窗顶部保留检查结论radio、问题描述textarea、现场说明textarea及字数统计；
  - 底部分暂存/提交按钮，提交校验检查结论+全部检查项+清空子项状态；
  - 站点切换时同步更新弹窗内容并重置子项显示状态。
- 调整弹窗内问题顺序：检查结论/问题描述/现场说明移至7项检查项之后。
  - 改造 renderNoticePanel 为表单样式 + 不合格联动逻辑：
    - renderNoticePanel 从旧版文书卡片样式改为表单样式（.mc-notice-form-row/.mc-notice-form-item等CSS类），字段包含：站点名称、被检查单位、检查人员、联系方式、检查日期、通知日期、整改内容（textarea）、整改意见/要求（textarea）、被检查单位人员、签字日期 + 暂存/提交按钮；
    - 不合格联动：检查记录单提交时遍历7项检查项radio，检测到任意"否" → Toast提示"存在不合格项，请填写整改通知单" → 关闭弹窗 → 自动切换到整改通知单Tab（switchMainTab('notice')） → 表单边框红色闪烁5秒高亮提示；
    - switchMainTab 工具函数：按tabId切换3个主Tab（普查信息/检查记录单/整改通知单）；
    - bindNoticeEvents：整改通知单事件绑定，提交时校验整改内容/整改意见/被检查单位人员必填；
    - 添加 .mc-btn-default CSS；
    - 站点Tab切换时重新绑定 bindNoticeEvents。
- 按截图实现填写后的展示态：
  - 检查记录单Tab：已提交时显示卡片（标题+站点名+不合格/已提交标签+核查摘要+问题描述+检查项结果表格+检查人/日期），未提交时保留按钮；
  - 整改通知单Tab：已提交时显示截图2卡片（通知单编号/部门/站点名称/用户编码/管理部片区所/用热地址+整改事项灰底块+整改意见灰底块+整改期限/被检查单位人员/检查人员/联系方式/通知日期/状态+导出/编辑按钮），未提交时显示表单；
  - 新增 taskCheckState 状态对象保存提交数据，首站默认已提交；检查记录单弹窗提交后保存并刷新；通知单表单提交后保存并刷新；支持「编辑」切回表单态、「查看整改通知书」切换Tab。
- 双专家确认机制：检查记录单和整改通知单展示态底部增加确认区域，支持第一专家/第二专家依次确认，双方都确认后标记"已生效"。
- 合格站点隐藏整改通知单Tab：renderTabSystem 动态判断 showNotice（结论不合格或通知单已提交时显示），结论合格时不展示整改通知单Tab。
- 新增实测复核Tab：renderVerificationPanel 过滤普查依据包含"实测"的建筑物，每栋显示照片上传（模拟文件选择）和复查面积输入框，底部保存按钮。
- 同步更新 4 个 docs 文档（增量）：
  - 页面清单.md：更新 my-task-check.html 描述行 + 弹窗清单新增检查记录单弹窗 + 跳转关系图新增新Tab/确认流程
  - 字段字典.md：更新 6.6/6.7/6.8/6.9 节为弹窗版+展示态+表单/展示双态字段 → 新增 6.11 双专家确认字段 + 6.12 实测复核字段 + 更新待确认/已确认项
  - 业务流程.md：更新三、专家执行检查流程（7-10阶段拆分为多子阶段） → 更新分支流程（不合格自动跳转/合格隐藏Tab/双态切换/双专家确认/实测复核） → 更新待确认/已确认项
  - 交互规则.md：更新 6.3 内容Tab切换（动态渲染）、6.4 检查记录单交互（弹窗版+展示态+双专家确认）、6.5 整改通知单交互（表单/展示双态） → 新增 6.9 实测复核交互 + 6.10 双专家确认交互 → 更新 Toast 提示和禁用态规则 + 更新待确认/已确认项

- 普查明细表操作列新增 sticky 固定效果：
  `position: sticky; right: 0; z-index: 2` + 左侧阴影 `box-shadow: -2px 0 6px rgba(0,0,0,0.08)`，确保横向滚动时"详情"按钮始终可见；`min-width: 1500px` 确保列宽触发横向滚动。
- 按截图重新调整整改通知书（表单/展示双态）：
  - 表单字段重排：编号(系统自动生成/只读) + 部门(片区所/只读) → 整改事项(textarea，占位符按截图) → 整改期限(工作日/数字输入/默认5) → 整改意见(textarea，占位符按截图) → 被检查单位人员(预填普查人员) + 检查人员(赵立成/只读/自动生成) → 检查人员联系方式(必填输入)。
  - 移除旧字段：站点名称、被检查单位、检查日期、通知日期、签字日期。
  - 检查人员由可编辑改为只读；整改内容→整改事项；整改意见/要求→整改意见；联系方式→检查人员联系方式。
  - 展示态同步调整：编号+部门 → 整改事项灰底块 → 整改意见灰底块 → 整改期限(工作日)+被检查单位人员+检查人员+检查人员联系方式+状态。
  - 提交校验同步更新：校验整改事项/整改意见/被检查单位人员/检查人员联系方式。
  - 初始 mock notice 数据同步清理旧字段，deadlineDays 由 7 改为 5。


- 新增页面 `prototypes/rectification/review-notice.html`：整改通知书审核列表页
  - 页面结构：面包屑（首页/整改通知书审核）→ 页面主标题 → 筛选条件卡片（任务/站点输入+审核状态下拉+重置+查询）→ 列表标题+共2条记录徽标 → 11列表格（含操作列文字按钮）→ 分页栏
  - 2条Mock数据：待审核（黄色Tag+审核按钮）/ 审核通过（绿色Tag+详情按钮）
  - 筛选支持按关键字和状态过滤，数据响应式更新
  - CSS前缀：.rn- 隔离；引用 pc-system-template.css 基础组件
  - 已同步更新 docs/页面清单.md 新增页面行 + 跳转关系图


- `config.js`：去掉「审核管理」分组标题，`整改通知书审核`菜单项归入默认分组，排在`我的面积检查任务`之后。
- `review-notice.html` 三项修改：
  1. 操作列 sticky 固定（`position:sticky;right:0` + 阴影 + 背景色同步）
  2. 前置条件：mock 数据改用 deadlineDays（45天/15天），通过 deriveStatus() 自动推导——>30天→待审核（黄色Tag+审核按钮），≤30天→审核通过（绿色Tag+详情按钮）
  3. 修复 CSS 类名重复问题（rn-rn-col-center → rn-col-center）


- `review-notice.html` 新增整改通知书审核弹窗：
  - 点击列表操作列「审核」按钮 → 弹窗展示整改通知书详情（任务名称/站点名称/换热站编码/管理部/整改期限/当前状态）
  - 弹窗中部：审核结果（通过/驳回 radio，必填）+ 审核意见（textarea）
  - 弹窗底部：取消 + 确认按钮；确认时校验审核结果必选项，Toast 提示审核结果
  - 点击「详情」按钮 → 弹窗隐藏审核区域，仅展示详情（只读模式）
  - 弹窗支持点击遮罩/关闭按钮/取消按钮关闭，关闭时重置表单
  - CSS：rn-modal- 前缀隔离，overlay/card/header/body/footer 完整结构

- 修复 JS 语法错误：`openReviewModal` 中 `grid.innerHTML` 的多行字符串使用了 `\`（双反斜杠=转义反斜杠）导致续行失效，改为 `\`（单反斜杠=续行符），页面试渲染空白问题消除。

- 新增 `prototypes/rectification/rectification-task.html`：整改任务列表页
  - config.js：新增 pageMap.rectification-task + menuConfig 菜单项（🔧 整改任务）
  - 7个筛选条件：任务名称/站点名称/整改状态/整改人/管理部/片区所/是否超期（4列Grid）
  - 9列表格+操作列sticky固定：序号/主任务名称/站点名称/整改人/整改状态(5色Tag)/整改截止时间/是否超期(红绿)/当前处理节点/操作(详情+整改)
  - 6条Mock数据覆盖全部5种状态：待整改/待审核/待复查/已归档/已超期
  - CSS前缀：.rt- 隔离；操作列含详情文字按钮，待整改/已超期额外显示整改按钮+分隔线
  - 筛选支持7个维度匹配，重置一键清空
- docs/页面清单.md 新增页面行


- 基于 git status 增量同步更新 docs/ 下 4 个文档：
  - 页面清单.md：新增 rectification-detail.html 页面行 + 跳转关系图 + 弹窗清单（整改通知审核弹窗/整改信息弹窗/普查详情弹窗/整改审核弹窗）+ 待确认项
  - 业务流程.md：新增四、整改通知书审核流程（含前置规则>30天需审核）+ 五、整改任务追踪流程（待整改→待审核→待复查→已归档/已超期）+ 审核与整改意见联动规则 + 更新待确认项
  - 交互规则.md：新增七、整改通知书审核（7.1筛选/7.2表格操作/7.3前置条件）+ 八、整改任务列表（8.1筛选/8.2表格操作/8.3超期规则/8.4操作显隐）+ 九、整改任务详情（9.1审核弹窗联动/9.2Tab系统/9.3普查详情弹窗）
  - 字段字典.md：新增八、review-notice（筛选/表格/弹窗字段）+ 九、rectification-task（筛选/表格/弹窗字段）+ 十、rectification-detail（基础信息/普查明细/审核弹窗字段）+ 整改任务状态枚举表


- 新增页面 `prototypes/rectification/site-detail-list.html`：面积检查站点明细
  - config.js：新增 pageMap.site-detail-list + menuConfig 菜单项（📊 面积检查站点明细），排在整改任务之后
  - 筛选条件（2行4列Grid共8个）：站点名称/任务名称/检查状态/本轮次是否普查/检查结果/整改状态/检查完成时间/整改完成时间
  - 16列表格：序号/换热站编码/换热站名称/检查状态/管理部/片区所/站点类型/办事处/用地地址/本轮次是否普查/建筑年代/检查专家/检查完成时间/检查结果(合格/不合格)/整改状态/整改完成时间
  - 8条Mock数据覆盖全部检查状态（待检查/检查中/已检查/待整改）+ 全部检查结果（合格/不合格）+ 全部整改状态（—/待整改/待审核/待复查/已归档/已超期）
  - CSS前缀：.sd- 隔离；状态标签用独立class（sd-status-check-*/sd-result-*/sd-rectify-*）
  - 筛选支持8个维度匹配（输入模糊匹配+下拉精确匹配），重置一键清空，数据响应式更新

- site-detail-list.html 操作列增强：
  - 17列末尾新增操作列（sticky 固定），所有行显示「详情」链接（跳转 site-detail.html?code={编码}）
  - 站点类型为「自管站」时额外显示「明细导出」链接（与详情用竖线分隔）
  - 头部新增「导出」按钮（primary 蓝色），点击打开新页签展示「合同面积变化台账」Excel 风格表格（黑边框/居中/黄色高亮/合计行加粗）
  - 自管站点击「明细导出」打开新页签展示「{站点名}换热站合同面积变化台账」，列名替换为：客户卡号(自管站)/用户名称，其余列与导出一致（原/现核查表面积/面积变化/变化比例/增加居民/增加非居民/查增建筑物）
  - 明细导出使用事件委托绑定（.sd-export-detail class + data-site-name 属性），筛选后自动生效

- 业务完整性和交互修复（全面检查 + P0/P1/P2 修复）：
  - P0: task-detail.js — 绑定「查看整改任务」按钮跳转至 rectification-task.html
  - P0: site-detail.js — 返回链接增加 taskId 参数（task-detail.js 传参，site-detail.js 读取并拼接）
  - P1: review-notice.html — 修复筛选后操作列采用序号索引（改用 data-i 属性+currentData 变量）
  - P1: my-task-list.js — 已结束任务「查看详情」补全 ?id={taskId} 参数
  - P2: 全部分页按钮（6个页面）统一绑定 Toast 提示「当前为原型预览，分页未实现」
  - P2: rectification-detail.html — 4个占位 Tab 替换为完整 mock 数据卡片（检查记录单含7项检查表格、整改通知单含完整通知卡片、整改告知书含附件下载）
  - P2: site-detail.js — 整改告知书下载链接绑定 Toast 提示
  - 修复 3 处 Toast 函数名不匹配 bug（showRnToast→showToast、showMcToast→showToast、showRdToast→showToast）
  - config.js 补全 rectification-detail pageMap 条目（面包屑：首页/整改任务/任务详情）

- 数据清理与状态覆盖保障：
  - mock/data.js: 任务从 9 条精简为 4 条，每状态保留 1 条（草稿/待开始/进行中/已结束）；taskSites 清理冗余任务关联和 task:3 中的重复站点编码
  - my-task-list.js: 任务从 9 条精简为 6 条（待确认×2、进行中×2（含转派）、待开始×1、已结束×1）；计数改为动态取 myTasks.length
  - my-task-check.html: 修复 60218B002 站点编码名称冲突（原「裕华区万达站」→「长安区东站」，与 mock/data.js 一致），同步更新 siteDetailMap
  - 最终状态覆盖：所有页面每个状态/枚举值至少 1 条数据，数据链路可完整流转

- 创建专家App移动端框架：
  - 重构 expert-app-preview.html 为框架页面（data-page + app.js 注册），完整保留左侧菜单栏
  - 新增 pages/expert-app-preview.js 页面模块（手机模型机 + 文档面板）
  - config.js 新增 pageMap.expert-app-preview + 菜单 key；app.js 注册 PageExpertAppPreview
  - 全面采用 Ant Design Mobile 设计规范重写三个移动端页面：主色 #1677ff，卡片圆角12px
  - expert-app.html：导航栏 + 页面头部 + 双卡片 + 底部标签栏
  - expert-app-task-list.html：导航栏 + 下划线Tab筛选 + 列表卡片 + 底部标签栏
  - expert-app-task-detail.html：导航栏 + 状态卡片 + 信息区块 + 站点卡片 + 胶囊按钮

- expert-app-check-record.html 底部新增「电子签名」卡片：
  - 虚线边框签名区域（圆角8px、白色背景、居中显示"张建国"22px字体）
  - 下方提示文字"提交记录前请完成第一名专家确认签字"
  - 点击签名区域跳转 expert-app-signature.html
- 新增 expert-app-signature.html（电子签名页）：
  - 导航栏"电子签名"+返回按钮
  - 大面积白色签名画布（Canvas，支持鼠标/触摸手写）
  - 提示文字"请在上方空白处书写您的名字"
  - 底部双按钮：清除重写（白底红字）/ 提交签字（蓝底白字）
  - 提交时校验是否已书写，未书写提示"请先书写签名"
  - 提交成功后 Toast + history.back() 返回检查记录单

  - 页面清单.md：更新 expert-app-task-list行（segment→圆角胶囊+搜索栏），新增 expert-app-site-list/site-detail/building-detail/check-record 4行，更新跳转图（完整移动端链路），弹窗清单新增移动端审核弹窗，更新待确认项
  - 字段字典.md：更新13.2（搜索栏+segment切换），新增13.5站点列表/13.6站点详情/13.7楼栋详情/13.8检查记录单
  - 业务流程.md：新增八.专家App移动端流程（9阶段：工作台→任务列表→搜索筛选→确认/详情→站点列表→站点详情→楼栋详情→检查记录→整改审核）
  - 交互规则.md：更新10.3（segment切换+搜索），新增10.7站点列表/10.8站点详情/10.9楼栋详情/10.10检查记录单，更新待确认项


- expert-app-standalone.html 按 area_checker_static/index.html 一比一复刻：
  - 手机壳框架 (.app-phone-shell)：刘海+状态栏+屏幕+Home条
  - 完整复刻 app.css 样式（424行）：app-task-card/rt-card/sl-card/sd-main-card/app-entry-card 等
  - 9个App页面：首页/任务列表/任务确认/整改任务/站点列表/站点详情/检查记录单/楼栋详情
  - mock数据：5条面积检查任务+6条整改任务+4个站点
  - 导航系统：navTo/page切换/segment切换/Toast
  - 右侧说明区
  - 顶部新增统计卡片：楼号名称+蓝色状态标签（实地核查）+ 4格统计（原面积/现面积/面积变化/变化率）+ 底部元信息行（用热性质/收费类别/变更明细条数）
  - 基础信息Tab改为「楼栋基础信息」标题+12行信息（楼号名称/建筑年代/收费类别/普查依据类型/原面积/面积变化/依据附件(蓝色链接)/备注/层数/用热性质/控制方式/暖气类型/现面积）
  - 移除旧的楼栋信息卡片和实测复核照上传区
  - 导航栏标题固定为「楼栋详情」


- 基于 git status 增量同步更新 docs/ 下 4 个文档：
  - 页面清单.md：更新expert-app-task-list行（整改rt-card+待复查/已归档segment）、site-detail行（普查依据+实测复核弹窗+平面图/门头图预览卡片）、building-detail行（统计卡片+楼栋基础信息12行+变更详情卡片）、check-record行（电子签名跳转）、signature行（水印+提交校验）；新增standalone行（9页面复刻）；跳转图补充standalone和signature水印细节；待确认项补充
  - 字段字典.md：更新13.6 site-detail（普查依据+实测复核弹窗字段）、13.7 building-detail（顶部统计卡片+12行信息+变更详情卡片）、13.8 check-record（电子签名卡片）、新增13.9 signature（5个字段区域）
  - 业务流程.md：更新八.专家App流程 stage 7/8（楼栋详情统计卡片+签名页流程）；待确认项补充
  - 交互规则.md：更新10.8（实测复核弹窗4条规则）、10.9（依据附件点击）、10.10（电子签名卡片跳转）；待确认项补充2条
