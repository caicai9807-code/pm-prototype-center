## 2026-07-07 21:50

### 建筑物卡片优化 + 复核弹窗完整实现

**文件：** `prototypes/rectification/expert-app-site-detail.html`

**最终布局：** 方案② - 单列 2x2 网格 + 面积横排双卡

**CSS 新增：**
- `bld-grid` 2x2 网格，`bld-area-row` flex 横排双卡
- 面积卡片：灰底（建筑面积）+ 绿底/红底（面积变化），居中文字
- Toast 提示样式（`.toast`），支持 warning/success 两种

**复核弹窗功能（`openReviewModal`）：**
- 顶部：标题"建筑物复核" + 关闭按钮
- 建筑信息提示行：楼栋名称 + 普查面积
- 复核照片：虚线框点击上传（`<input type="file" accept="image/*" multiple>`），72px 缩略图预览 + 删除
- 复查面积：number 输入框（step=0.01），默认预填普查面积
- 校验：至少 1 张照片 + 有效面积 > 0
- 提交后保存到 `reviewDisplay[buildingId]`（含 photos 数组、area、date）
- 自动刷新建筑物卡片，"已复核"徽标更新显示面积 + 照片数量
- 遮罩层点击关闭

**基础补充：**
- 新增 `_reviewBuildings` 全局映射（buildingId → 数据）
- 新增 `_currentSiteData` 全局站点引用，供卡片刷新用
- 补全 Toast 基础设施（CSS + HTML + `showToast` 函数）

### 文档同步更新（docs/ 4 份）

同步 `git diff` 变更到 4 份业务文档（增量更新，仅追加新内容）：

**1. `docs/页面清单.md`**（+8/-3）
- 更新 expert-app-site-detail 页面描述：方案②单列布局、复核按钮、复核弹窗
- 弹窗清单新增「建筑物复核弹窗」条目
- 待确认项更新：照片上传、持久化、跨站点刷新

**2. `docs/字段字典.md`**（+29/-2）
- 更新 13.6 建筑物Tab为方案②布局描述
- 新增 13.10 建筑物复核弹窗字段（含全部表单字段和数据存储格式）
- 标注复核照片上传、持久化等待确认项

**3. `docs/业务流程.md`**（+10/-3）
- 更新流程第6步（查看站点详情）加入方案②卡片和复核按钮描述
- 新增流程 6a（建筑物复核）详细步骤
- 分支流程新增「建筑物复核提交」分支
- 待确认项更新

**4. `docs/交互规则.md`**（+19/-3）
- 更新 10.8 建筑物卡片交互：方案②布局、复核按钮、复核弹窗完整交互规则（7条新条目）
- Toast 规则新增：复核照片为空、复核面积无效、复核提交成功
- 待确认项更新：布局验证、持久化、真实上传、刷新逻辑
